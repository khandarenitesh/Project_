import { Companythreshold } from './companythreshold.model';

describe('Companythreshold', () => {
  it('should create an instance', () => {
    expect(new Companythreshold()).toBeTruthy();
  });
});
